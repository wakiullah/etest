/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cdn.pixabay.com', 'https://pixabay.com', 'images.fineartamerica.com', 'th-thumbnailer.cdn-si-edu.com'],
    },
};

export default nextConfig;
